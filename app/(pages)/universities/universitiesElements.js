import styled from 'styled-components';

export const Main = styled.div `
    width : 100%;
    height : 80vh;
    display : flex;
    flex-direction : column;
    justify-content : flex-start;
    align-items : center;
    background: linear-gradient(90deg, black, #01abcc);
    gap: 30px;
`

export const UniversitySectionTitle = styled.h3 `
    width: 100%;
    padding: 30px;
    font-size: 40px;
    font-family: corbel;
    letter-spacing: 8px;
`

